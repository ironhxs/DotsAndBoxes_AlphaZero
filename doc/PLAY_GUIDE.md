# 🎮 Dots and Boxes - 游戏对战完整指南

## 📖 目录

- [快速开始](#快速开始)
- [游戏模式](#游戏模式)
- [坐标系统](#坐标系统)
- [完整参数](#完整参数)
- [使用示例](#使用示例)
- [常见问题](#常见问题)

---

## 🚀 快速开始

### 安装要求
```bash
# 在 WSL 环境中
conda activate alphazero
cd /mnt/d/DotsAndBoxes_AlphaZero
```

### 最简单的开始方式

```bash
# 人机对战（最推荐）
python cli/play_ultimate.py --mode human

# 看 AI 自己下一局
python cli/play_ultimate.py --mode ai --num-games 1

# 测试两个不同搜索深度的 AI
python cli/play_ultimate.py --mode dual-ai --simulations 50 --simulations2 200 --num-games 10
```

---

## 🎯 游戏模式

### 1️⃣ Human Mode - 人机对战

**最适合**：学习游戏、测试 AI 强度、娱乐

```bash
# 基础对战（你先手）
python cli/play_ultimate.py --mode human

# AI 先手（更有挑战性）
python cli/play_ultimate.py --mode human --ai-first

# 调整 AI 难度
python cli/play_ultimate.py --mode human --simulations 50   # 简单
python cli/play_ultimate.py --mode human --simulations 100  # 中等（默认）
python cli/play_ultimate.py --mode human --simulations 200  # 困难
```

**游戏中可用命令**：
- **坐标输入**：`h 1 0`（横边）或 `v 2 1`（竖边）
- **数字输入**：`5`（直接输入动作编号）
- `moves` - 显示所有可选的边（带坐标）
- `hint` - AI 推荐最佳动作
- `top5` - 查看 AI 推荐的前5个动作
- `eval` - 评估当前局面优势
- `quit` - 退出游戏

**示例对话**：
```
你的回合！
请输入动作: hint

💡 AI 正在分析...
   推荐: 动作 15 = h 2 3
   说明: 横边 h 2 3 (点(2,3)到点(2,4))
   胜率: 67.5%

请输入动作: h 2 3
✓ 执行: 动作 15 = h 2 3
   横边 h 2 3 (点(2,3)到点(2,4))
```

---

### 2️⃣ AI Mode - AI 自我对弈

**最适合**：测试模型稳定性、快速验证训练效果

```bash
# 单局详细显示
python cli/play_ultimate.py --mode ai --num-games 1

# 批量测试（20局统计）
python cli/play_ultimate.py --mode ai --num-games 20

# 导出所有对局记录
python cli/play_ultimate.py --mode ai --num-games 10 --export
```

**输出示例**：
```
================================================================================
对战统计
================================================================================
总局数: 20
AI-1 胜: 11 (55.0%)
AI-2 胜: 8 (40.0%)
平局: 1 (5.0%)
================================================================================
```

---

### 3️⃣ Dual-AI Mode - 双 AI 对战 ⭐ 新功能

**最适合**：
- 对比不同训练阶段的模型
- 评估搜索深度的影响
- Arena 评估系统的替代方案

#### 场景 A：同一模型，不同搜索深度

```bash
# 快速 AI (50次) vs 深度 AI (200次)
python cli/play_ultimate.py --mode dual-ai \
    --checkpoint results/test_4060/latest.pth \
    --simulations 50 \
    --simulations2 200 \
    --num-games 20
```

**用途**：
- 测试搜索深度对胜率的影响
- 找到速度和性能的最佳平衡点
- 验证 MCTS 是否真的有效

**预期结果**：搜索越深的 AI 应该胜率越高

---

#### 场景 B：不同模型对战

```bash
# 训练迭代 5 vs 迭代 10
python cli/play_ultimate.py --mode dual-ai \
    --checkpoint results/iteration_5/best.pth \
    --checkpoint2 results/iteration_10/best.pth \
    --num-games 50
```

**用途**：
- 评估训练进展
- 决定是否接受新模型
- 复现 Arena 评估结果

---

#### 场景 C：完整参数对比

```bash
# 旧模型（深搜索）vs 新模型（浅搜索）
python cli/play_ultimate.py --mode dual-ai \
    --checkpoint results/old_model.pth \
    --checkpoint2 results/new_model.pth \
    --simulations 200 \
    --simulations2 100 \
    --num-games 100 \
    --export
```

**用途**：
- 研究模型质量 vs 搜索深度的权衡
- 看新模型是否足够强，即使搜索更少

---

## 🗺️ 坐标系统详解

Dots and Boxes 是在点阵上连边的游戏。5x5 棋盘有 **6x6=36 个点**。

### 横边 (Horizontal Edges)

**格式**：`h 行号 列号`

**含义**：连接 `(行,列)` 和 `(行,列+1)` 两个点的横线

**示例**：
```
     列 0   1   2   3   4
行 0   •───•───•───•───•
       h00 h01 h02 h03

行 1   •───•───•───•───•
       h10 h11 h12 h13

h 1 2 表示：连接点(1,2)和点(1,3)的横边
```

**范围**：
- 5x5 棋盘：`h [0-5] [0-4]`（6行 × 5列 = 30条横边）

---

### 竖边 (Vertical Edges)

**格式**：`v 行号 列号`

**含义**：连接 `(行,列)` 和 `(行+1,列)` 两个点的竖线

**示例**：
```
     列 0  1  2  3  4  5
行 0   •  •  •  •  •  •
       ↓  ↓  ↓  ↓  ↓  ↓
      v00 v01 v02 v03 v04

行 1   •  •  •  •  •  •
       ↓  ↓  ↓  ↓  ↓  ↓
      v10 v11 v12 v13 v14

v 2 1 表示：连接点(2,1)和点(3,1)的竖边
```

**范围**：
- 5x5 棋盘：`v [0-5] [0-5]`（6行 × 6列 = 36条竖边）

---

### 动作编号映射

程序内部使用数字编号（0-65），但用户可以用坐标：

| 坐标输入 | 动作编号 | 说明 |
|---------|---------|------|
| `h 0 0` | 0 | 第一条横边 |
| `h 0 1` | 1 | 第二条横边 |
| `h 5 4` | 29 | 最后一条横边 |
| `v 0 0` | 30 | 第一条竖边 |
| `v 0 1` | 31 | 第二条竖边 |
| `v 5 5` | 65 | 最后一条竖边 |

**查看所有可选边**：游戏中输入 `moves` 命令

---

## ⚙️ 完整参数列表

```bash
python cli/play_ultimate.py [选项]
```

### 游戏模式
| 参数 | 可选值 | 默认值 | 说明 |
|------|--------|--------|------|
| `--mode` | `human` / `ai` / `dual-ai` | `human` | 游戏模式 |

### 模型配置
| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `--checkpoint` | 路径 | `results/test_4060/latest.pth` | AI-1 的模型路径 |
| `--checkpoint2` | 路径 | 同 checkpoint | AI-2 的模型路径（仅 dual-ai） |

### MCTS 配置
| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `--simulations` | 整数 | 100 | AI-1 的 MCTS 模拟次数 |
| `--simulations2` | 整数 | 同 simulations | AI-2 的 MCTS 模拟次数（仅 dual-ai） |

### 对战配置
| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `--human-first` | 标志 | True | 人类先手（默认） |
| `--ai-first` | 标志 | False | AI 先手 |
| `--num-games` | 整数 | 1 | AI 对战局数 |
| `--export` | 标志 | False | 导出对局记录为 JSON |

---

## 💡 使用示例

### 示例 1：新手学习

```bash
# 简单 AI + 随时获取提示
python cli/play_ultimate.py --mode human --simulations 50

# 游戏中操作
请输入动作: moves      # 查看所有可选边
请输入动作: hint       # 获取 AI 建议
请输入动作: h 1 0      # 执行推荐的动作
```

---

### 示例 2：挑战困难 AI

```bash
# AI 先手 + 高搜索深度
python cli/play_ultimate.py --mode human --ai-first --simulations 300
```

---

### 示例 3：快速测试新训练的模型

```bash
# 10 局自我对弈看稳定性
python cli/play_ultimate.py --mode ai --checkpoint results/new_model.pth --num-games 10
```

---

### 示例 4：正式评估两个模型

```bash
# 100 局对战 + 导出记录
python cli/play_ultimate.py --mode dual-ai \
    --checkpoint results/baseline.pth \
    --checkpoint2 results/improved.pth \
    --num-games 100 \
    --export
```

**输出统计**：
```
================================================================================
对战统计
================================================================================
总局数: 100
AI-1 胜: 42 (42.0%)
AI-2 胜: 56 (56.0%)
平局: 2 (2.0%)
================================================================================
```

**结论**：AI-2 显著更强（56% 胜率 > 55% Arena 阈值）

---

### 示例 5：研究搜索深度影响

```bash
# 测试 25, 50, 100, 200 次搜索的差异
for sim2 in 25 50 100 200; do
    echo "Testing $sim2 simulations..."
    python cli/play_ultimate.py --mode dual-ai \
        --simulations 100 \
        --simulations2 $sim2 \
        --num-games 30
done
```

---

### 示例 6：完整的模型发布流程

```bash
# 步骤 1: 自我对弈检查（快速）
python cli/play_ultimate.py --mode ai --checkpoint results/candidate.pth --num-games 20

# 步骤 2: 对比当前最佳模型（中等）
python cli/play_ultimate.py --mode dual-ai \
    --checkpoint results/current_best.pth \
    --checkpoint2 results/candidate.pth \
    --num-games 50

# 步骤 3: 正式评估（严格）
python cli/play_ultimate.py --mode dual-ai \
    --checkpoint results/current_best.pth \
    --checkpoint2 results/candidate.pth \
    --num-games 200 \
    --export

# 如果候选模型胜率 > 55%，接受为新的 best model
```

---

## ❓ 常见问题

### Q1: 坐标范围是什么？

**A**: 对于 5x5 棋盘（默认）：
- **横边**：`h [0-5] [0-4]`（6行，5列）
- **竖边**：`v [0-5] [0-5]`（6行，6列）

输入 `moves` 命令可以看到所有合法边。

---

### Q2: 为什么搜索深的 AI 反而输了？

**A**: 可能的原因：
1. **模型训练不足**：策略网络不准确，搜索反而放大错误
2. **过拟合**：模型在训练数据上过拟合，泛化能力差
3. **搜索参数**：MCTS 的 `cpuct` 参数可能需要调整

**验证方法**：
```bash
# 测试不同搜索深度
python cli/play_ultimate.py --mode dual-ai --simulations 50 --simulations2 100 --num-games 20
python cli/play_ultimate.py --mode dual-ai --simulations 100 --simulations2 200 --num-games 20
```

如果一直是浅搜索赢，说明模型质量有问题。

---

### Q3: dual-ai 模式有什么用？

**A**: 三大用途：

1. **训练验证**：
   - 快速测试新模型是否比旧模型强
   - 决定是否接受新训练的权重

2. **超参数调优**：
   - 测试不同 MCTS 模拟次数的影响
   - 找到速度和性能的最佳平衡

3. **科学研究**：
   - 研究搜索深度 vs 模型质量的关系
   - 对比不同训练策略的效果

---

### Q4: 如何调整 AI 难度？

**A**: 通过 `--simulations` 参数：

| 模拟次数 | 难度 | 响应速度 | 适用场景 |
|---------|------|---------|---------|
| 25-50 | 简单 | 快 | 新手学习、快速测试 |
| 100 | 中等 | 中 | 正常对战（默认） |
| 200-300 | 困难 | 慢 | 挑战高手、正式评估 |
| 400+ | 极难 | 很慢 | 研究用途 |

---

### Q5: 导出的记录文件在哪里？

**A**: `results/games/game_record_YYYYMMDD_HHMMSS.json`

**格式**：
```json
{
  "game_info": {
    "game": "Dots and Boxes",
    "rows": 5,
    "cols": 5,
    "date": "2025-11-12T16:30:45.123456",
    "total_moves": 42
  },
  "moves": [
    {"move_number": 1, "player": 0, "action": 15},
    {"move_number": 2, "player": 1, "action": 23},
    ...
  ]
}
```

---

### Q6: 如何验证训练效果？

**A**: 推荐流程：

```bash
# 1. 快速自测（5-10 局）
python cli/play_ultimate.py --mode ai --checkpoint results/new_model.pth --num-games 10

# 2. 对比基准（30-50 局）
python cli/play_ultimate.py --mode dual-ai \
    --checkpoint results/baseline.pth \
    --checkpoint2 results/new_model.pth \
    --num-games 50

# 3. 正式评估（100+ 局）
python cli/play_ultimate.py --mode dual-ai \
    --checkpoint results/old_best.pth \
    --checkpoint2 results/new_model.pth \
    --num-games 100 \
    --export
```

**判断标准**：
- 胜率 > 55%：新模型显著更强，接受
- 胜率 45-55%：差异不明显，继续训练
- 胜率 < 45%：新模型更弱，回退

---

### Q7: 人机对战时 AI 思考太久怎么办？

**A**: 降低搜索次数：

```bash
# 降低到 50 次（响应快）
python cli/play_ultimate.py --mode human --simulations 50

# 极速模式（25 次）
python cli/play_ultimate.py --mode human --simulations 25
```

---

### Q8: 如何保存重要对局？

**A**: 使用 `--export` 参数：

```bash
# 人机对战（你的精彩胜利）
python cli/play_ultimate.py --mode human --export

# AI 对战（研究用）
python cli/play_ultimate.py --mode dual-ai --num-games 1 --export
```

记录会保存为 JSON，可以用于：
- 回放分析
- 训练数据增强
- 论文插图

---

## 🎓 高级技巧

### 技巧 1：批量评估脚本

创建 `evaluate.sh`：
```bash
#!/bin/bash

# 测试不同搜索深度
for sim in 25 50 100 200 400; do
    echo "Evaluating $sim simulations..."
    python cli/play_ultimate.py --mode dual-ai \
        --checkpoint results/baseline.pth \
        --simulations 100 \
        --simulations2 $sim \
        --num-games 50 \
        >> results/depth_study.log
done
```

---

### 技巧 2：模型锦标赛

```bash
# 让所有模型两两对战
models=("iter_5" "iter_10" "iter_15" "iter_20")

for i in "${!models[@]}"; do
    for j in "${!models[@]}"; do
        if [ $i -lt $j ]; then
            echo "${models[$i]} vs ${models[$j]}"
            python cli/play_ultimate.py --mode dual-ai \
                --checkpoint results/${models[$i]}/best.pth \
                --checkpoint2 results/${models[$j]}/best.pth \
                --num-games 30
        fi
    done
done
```

---

### 技巧 3：使用 hint 学习策略

```bash
# 开局阶段多用 hint
python cli/play_ultimate.py --mode human --simulations 200

# 游戏中
请输入动作: hint       # 看 AI 推荐
请输入动作: top5       # 看前5个选择
请输入动作: eval       # 看局面评估
请输入动作: [执行 AI 推荐的动作]
```

通过观察 AI 的选择，学习：
- 哪些位置是关键点
- 如何控制盒子的完成时机
- 如何利用"连续下棋"规则

---

## 📊 性能参考

### 硬件：RTX 4060 Laptop (8GB)

| 模式 | MCTS 次数 | 每步耗时 | 一局耗时 |
|------|----------|---------|---------|
| Human | 50 | ~0.3s | - |
| Human | 100 | ~0.6s | - |
| Human | 200 | ~1.2s | - |
| AI vs AI | 100 | ~0.6s | ~25s |
| Dual-AI (50 vs 200) | 混合 | ~0.9s | ~35s |

**优化建议**：
- 日常测试：50-100 次模拟
- 正式评估：100-200 次模拟
- 研究用途：200-400 次模拟

---

## 🔗 相关文档

- [训练指南](HOW_TO_TRAIN.md) - 如何训练新模型
- [AlphaZero 原理](ALPHAZERO_EXPLAINED.md) - 算法详解
- [项目 README](../README.md) - 项目概述

---

## 🎉 快速参考卡

### 最常用命令

```bash
# 人机对战（默认）
python cli/play_ultimate.py

# AI 自测（20局）
python cli/play_ultimate.py --mode ai --num-games 20

# 双 AI 对比（50局）
python cli/play_ultimate.py --mode dual-ai \
    --checkpoint model1.pth \
    --checkpoint2 model2.pth \
    --num-games 50

# 调整难度
python cli/play_ultimate.py --simulations [25|50|100|200]

# AI 先手
python cli/play_ultimate.py --ai-first

# 导出记录
python cli/play_ultimate.py --export
```

### 游戏中命令

| 命令 | 作用 |
|------|------|
| `h 行 列` | 放置横边 |
| `v 行 列` | 放置竖边 |
| `数字` | 直接输入动作编号 |
| `moves` | 显示所有可选边 |
| `hint` | AI 推荐 |
| `top5` | 前5推荐 |
| `eval` | 局面评估 |
| `quit` | 退出 |

---

**祝您游戏愉快！** 🎮✨

有问题请参考 [GitHub Issues](https://github.com/ironhxs/DotsAndBoxes_AlphaZero/issues)
